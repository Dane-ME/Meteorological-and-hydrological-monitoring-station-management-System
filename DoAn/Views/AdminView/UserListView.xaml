<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DoAn.Views.AdminView.UserListView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Background="#578FC8">
            <HorizontalStackLayout HorizontalOptions="Start" VerticalOptions="Center">
                <Label Text="STT" TextColor="#EAECF5" FontSize="18" Margin="15,0,0,0"></Label>
                <Label Text="Tên Đăng nhập" TextColor="#EAECF5" FontSize="18" Margin="15,0,0,0"></Label>
                <Label Text="Trạng thái" TextColor="#EAECF5" FontSize="18" Margin="100,0,0,0"></Label>
            </HorizontalStackLayout>
        </Grid>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <ListView>

            </ListView>
        </Grid>
        
    </Grid>
</ContentView>
