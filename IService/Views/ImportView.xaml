<UserControl x:Class="IService.Views.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IService.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="AliceBlue"> 
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Content="Khí tượng" x:Name="meteorology" Width="80" FontSize="16" Background="AliceBlue" BorderThickness="0" ></Button>
            <Button Content="Thủy Văn" x:Name="hydrological" Width="80" FontSize="16" Background="AliceBlue" BorderThickness="0" ></Button>
            <Button Content="Mưa" x:Name="rainfall" Width="80" FontSize="16" Background="AliceBlue" BorderThickness="0"> </Button>
        </StackPanel>
        <Border Grid.Row="1" x:Name="MainContent"></Border>
    </Grid>
</UserControl>
